import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'parolette-alla-cecata',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
